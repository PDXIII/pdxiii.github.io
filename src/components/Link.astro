---
const { to, classList } = Astro.props;
---

<a
  class={`underline decoration-1 hover:decoration-black decoration-primary transition-colors hover:text-primary underline-offset-2 ${classList}`}
  href={to}
>
  <slot />
</a>
